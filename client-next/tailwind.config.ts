import type { Config } from 'tailwindcss';

// Tailwind CSS v4 uses CSS-first configuration. This stub exists for tooling
// (shadcn/ui CLI, IDE plugins) that still expect a config file path.
const config: Config = {
    content: [],
};

export default config;
